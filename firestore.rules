rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SIAKAD School Management System Cloud Database Rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
